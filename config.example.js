// Catatan: aplikasi ini SUDAH TIDAK memakai file config.js — API key ditulis
// langsung sebagai konstanta di script.js (lihat variabel API_KEY di baris atas).
// File ini dibiarkan sebagai contoh pola "config.js + .gitignore" yang lebih
// aman, kalau nanti kamu bikin proyek lain yang key-nya tidak boleh publik.
const CONFIG = {
  OPENWEATHER_API_KEY: "isi-api-key-kamu-di-sini",
};
